---
phase: 03-homepage-bespoke
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - bespoke.html
autonomous: true
requirements:
  - BP-01
  - BP-02
  - BP-03
  - BP-04
  - BP-05
  - WA-03
  - AN-01
must_haves:
  truths:
    - "Bespoke page has zero inline style blocks"
    - "All 5 chapter images use the Phase 1 .placeholder .placeholder--portrait pattern"
    - "All 4 fabric swatches use the Phase 1 .placeholder .placeholder--fabric pattern"
    - "All WhatsApp links use the real number 6588567038"
    - "CTA banner links to WhatsApp"
  artifacts:
    - path: "bespoke.html"
      provides: "Clean bespoke page with no inline styles, correct placeholders, correct WhatsApp links"
      contains: "wa.me/6588567038"
  key_links:
    - from: "bespoke.html"
      to: "css/style.css"
      via: "link rel stylesheet"
      pattern: "css/style\\.css"
    - from: "bespoke.html"
      to: "wa.me/6588567038"
      via: "href on CTA banner"
      pattern: "wa\\.me/6588567038"
---

<objective>
Clean up bespoke.html: remove the entire inline style block, migrate all placeholder classes to the Phase 1 pattern, update all WhatsApp links to the real number, and replace section heading inline styles with the utility class.

Purpose: The bespoke page HTML becomes clean markup referencing only external stylesheets. All dark-theme remnants including the #0d0905 gradient are gone (the CSS rule was already excluded from migration in Plan 01).

Output: Updated bespoke.html with no inline styles, correct placeholder markup, and real WhatsApp numbers.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-bespoke/03-CONTEXT.md
@.planning/phases/03-homepage-bespoke/03-PATTERNS.md
@.planning/phases/03-homepage-bespoke/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove inline styles and update WhatsApp numbers</name>
  <files>bespoke.html</files>
  <read_first>bespoke.html, css/style.css</read_first>
  <action>
**1. Remove the entire inline `<style>` block (lines 13-220).** Delete from `<style>` opening tag through `</style>` closing tag. The `</head>` tag should immediately follow the `<link rel="stylesheet" href="css/style.css">` line.

This removes all bespoke page-specific styles including:
- `.page-hero` and `.page-hero::before` (dark gradient #0d0905 -- gone, not migrated to style.css)
- `.chapter`, `.fabric-card`, `.promise-grid` etc. (all migrated to style.css in Plan 01 with corrected tokens)

**2. Update ALL `65XXXXXXXX` placeholder WhatsApp numbers to `6588567038`:**

There are 3 occurrences of `65XXXXXXXX` in bespoke.html (nav contact link line 232, nav overlay CTA line 234, CTA banner line 436). Replace each with `6588567038`. Also remove the `<!-- TODO: Replace 65XXXXXXXX ... -->` comment lines (lines 236, 435, 460).

The footer WhatsApp link at line 461 also has `65XXXXXXXX` -- replace it too with `6588567038`.

Total: 4 replacements of `65XXXXXXXX` to `6588567038`.
  </action>
  <verify>
    <automated>grep -c "<style>" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/bespoke.html && grep -c "65XXXXXXXX" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/bespoke.html && grep -c "wa.me/6588567038" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/bespoke.html; echo "Expected: 0, 0, >=3"</automated>
  </verify>
  <acceptance_criteria>
    - bespoke.html contains zero `<style>` tags
    - bespoke.html contains zero `65XXXXXXXX` placeholder numbers
    - bespoke.html contains zero `TODO: Replace` comments referencing WhatsApp
    - bespoke.html contains `wa.me/6588567038` at least 3 times (nav, CTA banner, footer)
    - bespoke.html does NOT contain `#0d0905` (dark gradient was in the removed inline style)
  </acceptance_criteria>
  <done>All inline styles removed from bespoke.html. All WhatsApp placeholder numbers replaced with 6588567038. Dark gradient is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate placeholder classes and fix section heading styles</name>
  <files>bespoke.html</files>
  <read_first>bespoke.html</read_first>
  <action>
**1. Migrate all 5 chapter image placeholders:**

Each chapter has a `<div class="chapter-image placeholder-img" data-label="..."></div>`. Replace each with the Phase 1 pattern:

```html
<div class="chapter-image placeholder placeholder--portrait">
  <img src="img/placeholders/silhouette-portrait.svg" alt="">
</div>
```

The 5 instances are in:
- Chapter 1 (Consultation): `data-label="Consultation photography"`
- Chapter 2 (Sketch): `data-label="Sketching process"`
- Chapter 3 (Toile): `data-label="Toile draping"`
- Chapter 4 (Craft): `data-label="Hand embellishment"`
- Chapter 5 (Reveal): `data-label="Final reveal photography"`

All 5 get the same replacement -- `placeholder--portrait` variant with `silhouette-portrait.svg`.

**2. Migrate all 4 fabric swatch placeholders:**

Each fabric card has a `<div class="fabric-swatch placeholder-img" data-label="Fabric macro"></div>`. Replace each with:

```html
<div class="fabric-swatch placeholder placeholder--fabric">
  <img src="img/placeholders/silhouette-fabric.svg" alt="">
</div>
```

The 4 instances are in the French Chantilly Lace, Lyonnaise Silk Duchesse, Italian Silk Organza, and Murano Glass Beadwork cards.

**3. Replace inline `style="margin-top: 16px; margin-bottom: 8px;"` on section h2 elements:**

There are 2 h2 elements with this inline style:
- Fabric section: `<h2 class="reveal" style="margin-top: 16px; margin-bottom: 8px;">The Fabric Archive</h2>`
- Promise section: `<h2 class="reveal" style="margin-top: 16px; margin-bottom: 8px;">What bespoke means here</h2>`

Replace each with:
```html
<h2 class="reveal section-heading">The Fabric Archive</h2>
<h2 class="reveal section-heading">What bespoke means here</h2>
```

The `.section-heading` utility class was added to style.css in Plan 01 with `margin-top: 16px; margin-bottom: 8px;`.
  </action>
  <verify>
    <automated>grep -c "placeholder-img" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/bespoke.html && grep -c "placeholder--portrait" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/bespoke.html && grep -c "placeholder--fabric" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/bespoke.html; echo "Expected: 0, 5, 4"</automated>
  </verify>
  <acceptance_criteria>
    - bespoke.html contains zero `placeholder-img` references
    - bespoke.html contains 5 occurrences of `placeholder--portrait` (one per chapter)
    - bespoke.html contains 4 occurrences of `placeholder--fabric` (one per fabric card)
    - bespoke.html contains 5 occurrences of `silhouette-portrait.svg`
    - bespoke.html contains 4 occurrences of `silhouette-fabric.svg`
    - No h2 elements have `style="margin-top: 16px; margin-bottom: 8px;"` inline (grep returns 0)
    - Both fabric and promise h2 elements have `class="reveal section-heading"`
  </acceptance_criteria>
  <done>All 9 placeholder elements migrated to Phase 1 pattern (5 portrait, 4 fabric). Section heading inline styles replaced with utility class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Same wa.me deep links as homepage.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | N/A | bespoke.html | accept | Static HTML with no user input -- no applicable STRIDE threats |
</threat_model>

<verification>
1. `grep -c "<style>" bespoke.html` returns 0
2. `grep -c "65XXXXXXXX" bespoke.html` returns 0
3. `grep -c "placeholder-img" bespoke.html` returns 0
4. `grep -c "placeholder--portrait" bespoke.html` returns 5
5. `grep -c "placeholder--fabric" bespoke.html` returns 4
6. `grep -c "wa.me/6588567038" bespoke.html` returns 3+
7. `grep "#0d0905" bespoke.html` returns 0 matches
8. Open bespoke.html in browser -- hero, chapters, fabric, promise, CTA all render on ivory background
</verification>

<success_criteria>
- bespoke.html has zero inline style blocks
- All 5 chapter images use .placeholder .placeholder--portrait with SVG
- All 4 fabric swatches use .placeholder .placeholder--fabric with SVG
- All WhatsApp numbers are real (6588567038)
- No dark-theme references (#0d0905, gold variables) remain
- Section headings use .section-heading utility class
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage-bespoke/03-03-SUMMARY.md`
</output>
