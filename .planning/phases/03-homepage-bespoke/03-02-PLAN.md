---
phase: 03-homepage-bespoke
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - index.html
autonomous: true
requirements:
  - HP-01
  - HP-02
  - HP-03
  - HP-04
  - HP-05
  - HP-06
  - WA-03
  - AN-01
  - AN-02
must_haves:
  truths:
    - "Homepage has zero inline style blocks"
    - "Hero CTA links to WhatsApp wa.me on both desktop and mobile"
    - "Desktop hero shows secondary text link to commission.html"
    - "Mobile hero hides the secondary CTA"
    - "All placeholder images use the Phase 1 .placeholder pattern with SVG"
    - "Process steps have individual .reveal classes for staggered animation"
    - "All WhatsApp links use the real number 6588567038"
  artifacts:
    - path: "index.html"
      provides: "Clean homepage with no inline styles, correct CTAs, correct placeholders"
      contains: "wa.me/6588567038"
  key_links:
    - from: "index.html"
      to: "css/style.css"
      via: "link rel stylesheet"
      pattern: "css/style\\.css"
    - from: "index.html"
      to: "wa.me/6588567038"
      via: "href on hero CTA"
      pattern: "wa\\.me/6588567038"
---

<objective>
Clean up index.html: remove the entire inline style block, restructure the hero CTA per D-06/D-07, migrate placeholder classes, fix reveal class assignments for stagger, update all WhatsApp links to the real number, and remove the video badge element.

Purpose: The homepage HTML becomes clean markup referencing only external stylesheets. All styles come from css/style.css (migrated in Plan 01). Hero conversion path goes directly to WhatsApp.

Output: Updated index.html with no inline styles, correct hero CTAs, correct placeholder markup, and staggered reveal classes.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-bespoke/03-CONTEXT.md
@.planning/phases/03-homepage-bespoke/03-PATTERNS.md
@.planning/phases/03-homepage-bespoke/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove inline styles and video badge, restructure hero CTA</name>
  <files>index.html</files>
  <read_first>index.html, css/style.css</read_first>
  <action>
Make the following changes to index.html:

**1. Remove the entire inline `<style>` block (lines 13-326).** Delete from `<style>` opening tag through `</style>` closing tag. The `</head>` tag should immediately follow the `<link rel="stylesheet" href="css/style.css">` line.

**2. Remove the video badge element (line 353):**
Delete this entire element:
```html
<div class="video-badge">Full-screen video . Atelier at work</div>
```
This is a deferred v2 feature (VD-01) per D-10/deferred items.

**3. Restructure the hero CTA section (current lines 357-359) per D-06 and D-07:**

Replace:
```html
<div class="hero-cta">
  <a href="commission.html" class="btn-outline">Begin your commission</a>
</div>
```

With:
```html
<div class="hero-cta">
  <a href="https://wa.me/6588567038?text=Hello%20%E2%80%94%20I%27d%20like%20to%20discuss%20a%20bespoke%20commission%20for%20%5Boccasion%5D."
     class="btn-primary">Begin your commission</a>
  <a href="commission.html" class="hero-secondary-cta">Learn about our process</a>
</div>
```

The `.hero-secondary-cta` is hidden on mobile via CSS (added in Plan 01). Desktop shows both CTAs; mobile shows only the WhatsApp button (per D-07).

**4. Update ALL `65XXXXXXXX` placeholder WhatsApp numbers to `6588567038`:**

There are 3 occurrences of `65XXXXXXXX` in index.html (nav contact link, nav overlay CTA, CTA banner). Replace each with `6588567038`. Also remove the `<!-- TODO: Replace 65XXXXXXXX ... -->` comment lines.

The footer WhatsApp link at line 513 already has `6588567038` -- leave it as-is.
  </action>
  <verify>
    <automated>grep -c "<style>" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/index.html && grep -c "video-badge" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/index.html && grep -c "65XXXXXXXX" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/index.html && grep -c "wa.me/6588567038" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/index.html; echo "Expected: 0, 0, 0, >=4"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains zero `<style>` tags (grep -c "<style>" returns 0)
    - index.html contains zero `video-badge` references
    - index.html contains zero `65XXXXXXXX` placeholder numbers
    - index.html contains `wa.me/6588567038` at least 4 times (nav contact, nav overlay CTA, hero CTA, CTA banner)
    - index.html contains `class="btn-primary">Begin your commission</a>` in the hero section
    - index.html contains `class="hero-secondary-cta">Learn about our process</a>` in the hero section
    - index.html contains `href="commission.html"` on the secondary CTA
    - index.html does NOT contain `btn-outline` in the hero section (hero CTA is now btn-primary)
  </acceptance_criteria>
  <done>Hero CTA restructured with WhatsApp primary + commission.html secondary. All inline styles removed. Video badge removed. All placeholder WhatsApp numbers replaced with real number.</done>
</task>

<task type="auto">
  <name>Task 2: Fix placeholder classes and reveal stagger assignments</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
**1. Migrate featured commission placeholder (around line 451 before style removal, now ~line 138 area):**

Replace:
```html
<div class="featured-image placeholder-img" data-label="Editorial photography"></div>
```

With:
```html
<div class="featured-image placeholder placeholder--portrait">
  <img src="img/placeholders/silhouette-portrait.svg" alt="">
</div>
```

**2. Fix process step reveal stagger (the .process-grid section):**

Current state: `<div class="process-grid reveal">` with plain `<div class="process-step">` children. This makes all 5 steps reveal simultaneously.

Change to: Remove `.reveal` from the `.process-grid` container. Add `.reveal` to each `.process-step` individually, with stagger delays on steps 2-5:

```html
<div class="process-grid">
  <div class="process-step reveal">
    ...step 1 content...
  </div>
  <div class="process-step reveal reveal-delay-1">
    ...step 2 content...
  </div>
  <div class="process-step reveal reveal-delay-2">
    ...step 3 content...
  </div>
  <div class="process-step reveal reveal-delay-3">
    ...step 4 content...
  </div>
  <div class="process-step reveal reveal-delay-4">
    ...step 5 content...
  </div>
</div>
```

Do NOT change the content inside each process-step -- only add the class attributes.

**3. Fix featured-grid reveal:**

Current state: `<div class="featured-grid reveal">` wraps the image and text. This is acceptable since both halves should reveal together. Leave this as-is.

**4. Replace inline style on the featured CTA div (around the "View the archive" button):**

Replace:
```html
<div style="margin-top: 32px;">
```

With:
```html
<div class="section-heading" style="margin-top: 32px;">
```

Actually, since we want to eliminate inline styles entirely, replace with a simple margin class. The simplest approach: just use the existing margin value inline since it's a one-off. Alternatively, keep the inline `style="margin-top: 32px;"` -- this is a layout value, not a dark-theme reference. Keeping one trivial inline margin is acceptable and not a D-05 violation (D-05 targets orphaned variable references, not all inline styles).

**5. Replace inline `style="margin-top: 16px; margin-bottom: 8px;"` on the two h2 elements (process and featured sections):**

Replace `style="margin-top: 16px; margin-bottom: 8px;"` with `class="section-heading"` (added to style.css in Plan 01) on both h2 elements. These h2 elements already have a `class="reveal"` -- merge to `class="reveal section-heading"`.

Process section h2:
```html
<h2 class="reveal section-heading">The Process</h2>
```

Featured section h2:
```html
<h2 class="reveal section-heading">From the Archive</h2>
```
  </action>
  <verify>
    <automated>grep -c "placeholder-img" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/index.html && grep "process-grid" /Users/justinlong/Documents/Clients/ttmd/ttmd-site/index.html | head -1; echo "Expected: 0 occurrences of placeholder-img; process-grid without reveal class"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains zero `placeholder-img` references
    - index.html contains `placeholder placeholder--portrait` in the featured image div
    - index.html contains `img/placeholders/silhouette-portrait.svg` inside the featured image placeholder
    - `process-grid` div does NOT have `reveal` class (grep for `class="process-grid reveal"` returns 0)
    - Each of the 5 `.process-step` divs has class `reveal` (grep -c `process-step reveal` returns 5)
    - Steps 2-5 have `reveal-delay-1` through `reveal-delay-4` respectively
    - The two section h2 elements use `class="reveal section-heading"` instead of inline style
  </acceptance_criteria>
  <done>All placeholder classes migrated to Phase 1 pattern. Process steps have individual reveal classes with stagger delays. Section heading inline styles replaced with utility class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> wa.me | WhatsApp deep link with pre-filled message; no user data collected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | S (Spoofing) | wa.me link | accept | WhatsApp link is a public deep link to a known business number; no authentication involved |
</threat_model>

<verification>
1. `grep -c "<style>" index.html` returns 0
2. `grep -c "video-badge" index.html` returns 0
3. `grep -c "65XXXXXXXX" index.html` returns 0
4. `grep -c "placeholder-img" index.html` returns 0
5. `grep -c "wa.me/6588567038" index.html` returns 4+
6. `grep "process-grid" index.html` does NOT contain "reveal"
7. `grep -c "process-step reveal" index.html` returns 5
8. Open index.html in browser -- hero, clock, process, featured, CTA all render correctly
</verification>

<success_criteria>
- index.html has zero inline style blocks
- Hero CTA links to WhatsApp with btn-primary class
- Secondary CTA links to commission.html with hero-secondary-cta class
- All WhatsApp numbers are real (6588567038)
- Featured image uses .placeholder .placeholder--portrait with SVG
- Process steps have individual .reveal with stagger delays
- Video badge element removed
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage-bespoke/03-02-SUMMARY.md`
</output>
