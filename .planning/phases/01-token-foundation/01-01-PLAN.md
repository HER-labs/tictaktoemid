---
phase: 01-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/tokens.css
  - css/style.css
  - index.html
  - bespoke.html
  - archive.html
  - commission.html
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04
  - DS-05

must_haves:
  truths:
    - "Browser renders warm ivory (#F8F6F3) background instead of dark (#0a0806)"
    - "Headings render in Cormorant Garamond, body renders in Jost"
    - "Typography scales fluidly with clamp() — no breakpoint overrides"
    - "Hover states visibly darken text (secondary to primary) with 500ms transitions"
    - "No gold/brass/accent colors visible anywhere — palette is monochromatic"
    - "No visible borders or dividers — whitespace alone separates sections"
  artifacts:
    - path: "css/tokens.css"
      provides: "Design token custom properties"
      contains: "--bg"
    - path: "css/style.css"
      provides: "Structural styles referencing new tokens"
      contains: "var(--text-secondary)"
  key_links:
    - from: "index.html"
      to: "css/tokens.css"
      via: "link element in head"
      pattern: "href=\"css/tokens.css\""
    - from: "css/style.css"
      to: "css/tokens.css"
      via: "var() references resolve to tokens"
      pattern: "var\\(--text-secondary\\)"
---

<objective>
Create the CSS design token system and transform the existing dark-theme stylesheet into the warm ivory monochromatic palette.

Purpose: Establish the visual foundation (color, typography, easing, interaction states) that every subsequent phase builds upon. After this plan, opening any HTML page in a browser shows the ivory palette with correct fonts loaded.

Output: `css/tokens.css` (new), `css/style.css` (transformed), all 4 HTML pages updated with new font loading and token import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-token-foundation/01-CONTEXT.md
@.planning/phases/01-token-foundation/01-PATTERNS.md
@.planning/phases/01-token-foundation/01-UI-SPEC.md

<interfaces>
<!-- Current css/style.css :root block (lines 9-23) — to be extracted and replaced -->
:root {
  --bg:        #0a0806;
  --bg-card:   rgba(232,224,208,0.025);
  --gold:      #BA7517;
  --gold-light:#FAC775;
  --gold-dim:  rgba(186,117,23,0.4);
  --text:      #e8e0d0;
  --text-dim:  rgba(232,224,208,0.5);
  --text-faint:rgba(232,224,208,0.25);
  --border:    rgba(232,224,208,0.06);
  --border-gold:rgba(186,117,23,0.2);
  --serif:     'Playfair Display', 'Georgia', serif;
  --sans:      'Inter', -apple-system, 'Helvetica Neue', sans-serif;
  --ease:      cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

<!-- Current HTML head pattern (identical in all 4 pages, lines 8-12) -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@300;400&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
<link rel="stylesheet" href="css/style.css">

<!-- Token rename map for style.css transformation -->
var(--text-dim)    -> var(--text-secondary)
var(--text-faint)  -> var(--text-tertiary)
var(--gold)        -> var(--text) or var(--text-secondary) per context
var(--gold-light)  -> var(--text)
var(--gold-dim)    -> var(--text-tertiary)
var(--border)      -> remove border declarations entirely
var(--border-gold) -> remove border declarations entirely
var(--bg-card)     -> var(--bg-surface)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tokens.css and transform style.css</name>
  <files>css/tokens.css, css/style.css</files>
  <read_first>css/style.css, .planning/phases/01-token-foundation/01-UI-SPEC.md, .planning/phases/01-token-foundation/01-PATTERNS.md</read_first>
  <action>
1. Create `css/tokens.css` with this exact content:

```css
/* ============================================================
   TTMD — Time Taken to Make a Dress
   Design Tokens — import before all other stylesheets
   ============================================================ */

:root {
  /* Color */
  --bg:              #F8F6F3;
  --bg-surface:      #F2EFEB;
  --text:            #1A1A1A;
  --text-secondary:  #4A4A4A;
  --text-tertiary:   #8A8A8A;

  /* Typography */
  --serif:  'Cormorant Garamond', 'Georgia', serif;
  --sans:   'Jost', -apple-system, 'Helvetica Neue', sans-serif;

  /* Spacing */
  --space-xs:  4px;
  --space-sm:  8px;
  --space-md:  16px;
  --space-lg:  24px;
  --space-xl:  32px;
  --space-2xl: 48px;
  --space-3xl: 64px;
  --space-4xl: 140px;

  /* Easing & Transition */
  --ease:       cubic-bezier(0.25, 0.46, 0.45, 0.94);
  --transition: 500ms var(--ease);

  /* Placeholder */
  --placeholder-opacity: 0.30;
}
```

2. Modify `css/style.css`:
   - DELETE the entire `:root { ... }` block (lines 9-23)
   - Replace ALL occurrences of `var(--text-dim)` with `var(--text-secondary)`
   - Replace ALL occurrences of `var(--text-faint)` with `var(--text-tertiary)`
   - Replace ALL occurrences of `var(--gold)` with `var(--text-secondary)` (these are accent uses — eyebrow labels, decorative elements)
   - Replace ALL occurrences of `var(--gold-light)` with `var(--text)`
   - Replace ALL occurrences of `var(--gold-dim)` with `var(--text-tertiary)`
   - Replace ALL occurrences of `var(--bg-card)` with `var(--bg-surface)`
   - Remove ALL declarations containing `var(--border)` or `var(--border-gold)` — delete the entire property line (e.g., `border-bottom: 1px solid var(--border);` becomes nothing)
   - Update `.section` rule: remove any `border-bottom` property
   - Update ALL hard-coded transition durations less than 500ms (e.g., `0.3s`, `0.4s`, `300ms`) to `500ms` to align with D-14 luxury timing
   - Update `.reveal` transition: change `0.8s` to `800ms` (keep as-is since 800ms > 500ms minimum)
   - Update nav link hover: ensure color transitions from `var(--text-tertiary)` to `var(--text-secondary)` on hover
   - Add/update `.btn-primary:hover` to use `background: #0D0D0D` (full black darkening per D-13)
   - Ensure body `line-height` is `1.75` (per UI-SPEC body token)
  </action>
  <verify>
    <automated>grep -c "var(--gold)" css/style.css | grep "^0$" && grep -c "var(--border)" css/style.css | grep "^0$" && grep -q "#F8F6F3" css/tokens.css && grep -q "var(--text-secondary)" css/style.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - css/tokens.css exists and contains `--bg: #F8F6F3`
    - css/tokens.css contains `--text: #1A1A1A`
    - css/tokens.css contains `--text-secondary: #4A4A4A`
    - css/tokens.css contains `--text-tertiary: #8A8A8A`
    - css/tokens.css contains `--serif: 'Cormorant Garamond'`
    - css/tokens.css contains `--sans: 'Jost'`
    - css/tokens.css contains `--transition: 500ms`
    - css/tokens.css contains `--placeholder-opacity: 0.30`
    - css/style.css does NOT contain `:root {`
    - css/style.css does NOT contain `var(--gold)`
    - css/style.css does NOT contain `var(--border)`
    - css/style.css does NOT contain `var(--text-dim)`
    - css/style.css does NOT contain `var(--text-faint)`
    - css/style.css contains `var(--text-secondary)`
    - css/style.css contains `var(--text-tertiary)`
    - No transition duration in css/style.css is less than 500ms (except 800ms reveal which stays)
  </acceptance_criteria>
  <done>Token file created with all 10+ design tokens. style.css purged of all dark-theme tokens and gold/border references, all var() references point to new token names, transition timing is 500ms+.</done>
</task>

<task type="auto">
  <name>Task 2: Update HTML head blocks for new fonts and token import</name>
  <files>index.html, bespoke.html, archive.html, commission.html</files>
  <read_first>index.html, bespoke.html, archive.html, commission.html</read_first>
  <action>
In ALL four HTML files (index.html, bespoke.html, archive.html, commission.html), find the font/CSS block in the `<head>` and replace it with:

```html
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600&family=Jost:wght@300;400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/tokens.css">
  <link rel="stylesheet" href="css/style.css">
```

Specifically:
- Change the Google Fonts URL from `Playfair+Display:wght@300;400&family=Inter:wght@300;400;500` to `Cormorant+Garamond:wght@300;400;500;600&family=Jost:wght@300;400;500`
- Add `<link rel="stylesheet" href="css/tokens.css">` BEFORE the `css/style.css` link
- Keep the two preconnect links unchanged

Do NOT modify anything else in the HTML files — only the `<head>` CSS/font block.
  </action>
  <verify>
    <automated>grep -l "Cormorant+Garamond" index.html bespoke.html archive.html commission.html | wc -l | grep "^4$" && grep -l "css/tokens.css" index.html bespoke.html archive.html commission.html | wc -l | grep "^4$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 HTML files contain `Cormorant+Garamond:wght@300;400;500;600`
    - All 4 HTML files contain `Jost:wght@300;400;500`
    - All 4 HTML files contain `href="css/tokens.css"` BEFORE `href="css/style.css"`
    - No HTML file contains `Playfair+Display`
    - No HTML file contains `family=Inter`
    - Preconnect links to fonts.googleapis.com and fonts.gstatic.com remain
  </acceptance_criteria>
  <done>All four HTML pages load Cormorant Garamond + Jost from Google Fonts CDN and import tokens.css before style.css.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN -> Browser | Google Fonts loaded from external CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Google Fonts CDN link | accept | Google Fonts is a trusted CDN; subresource integrity not supported for font CSS. Low risk for a static brochure site. |
</threat_model>

<verification>
Open any of the 4 HTML files in a browser:
1. Background is ivory (#F8F6F3), not dark
2. Headings render in Cormorant Garamond (visible serif with high stroke contrast)
3. Body text renders in Jost (geometric sans)
4. No gold/brass colors visible anywhere
5. No visible borders between sections
6. Hovering nav links shows darkening with slow (500ms) transition
</verification>

<success_criteria>
- tokens.css exists with the complete flat token set
- style.css has zero references to retired tokens (gold, border, text-dim, text-faint)
- All 4 HTML pages import tokens.css before style.css
- All 4 HTML pages load Cormorant Garamond + Jost
- Browser renders ivory background with monochromatic text hierarchy
</success_criteria>

<output>
After completion, create `.planning/phases/01-token-foundation/01-01-SUMMARY.md`
</output>
